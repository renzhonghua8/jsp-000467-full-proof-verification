# Verification records

Verification is pending. This directory will record actual completed checks,
immutable source references, and build/kernel/axiom evidence. A source audit
by itself is not a successful proof reproduction.

Generated full logs and the machine-readable source manifest are kept out of
the source repository. Release evidence, if published, must be tied to the
actual source revision checked and must distinguish pending from passed work.
